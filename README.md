# Digital Corner

Перейдите в папку проекта:

```bash
cd ваш_репозиторий
```

Скопируйте .env.example:

```bash
cp .env.example .env
cp .env ./backend/.env
```

Запустите проект:
```bash
docker-compose up -d
```
